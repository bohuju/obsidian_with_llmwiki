/**
 * Wiki CLAUDE.md 操作规范模板
 * 基于 obsidian-llm-wiki 项目的 CLAUDE.md 迁移
 */
export declare function generateClaudeMd(config: {
    vaultName: string;
    wikiRoot: string;
    pageTypes: string[];
}): string;
